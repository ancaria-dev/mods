org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Where installSacredMod copies a jar. Unset, it stays in the build directory.
# No path is committed here because it is different on every machine:
#   gradlew installSacredMod -PsacredDir="C:/Games/Sacred Gold"
